#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char Array[] = "_putchar\n";
int al = 0;

while (al < 9)
{
_putchar(Array[al]);
al++;
}

return (0);

}
